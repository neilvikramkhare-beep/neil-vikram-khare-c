#include <stdio.h>
int main() 
{
    int i;
    int n;
    printf("Enter the value of n:");
    scanf("%d",&n);
    for (i = 10; i <= 20; i++) 
    {
        if (i%2==0)
        {
            printf("%d\n", i);
        }
    }
    return 0;
}
