#include <stdio.h>
int main()
{
    int n;
    long long sum; 
    printf("Enter the value of n: ");
    scanf("%d", &n);
    long long N = 5 * n; 
    sum = N * (N + 1) / 2;
printf("The sum of the first %d natural numbers is: %d\n", (int)N, sum);
    return 0;
}
