#include <stdio.h>
int main() 
{
    int a;
    int b;
    printf("Enter the value of first number: ");
    scanf("%d",&a);
     printf("Enter the value of second number: ");
     scanf("%d",&b);
      if(a>b)
      {
          printf("The first number is greater:",&a);
      }
          else
          {
              printf("The second number is greater:",&b);
          }
    return 0;
}
