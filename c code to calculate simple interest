#include <stdio.h>
int main()
{
int p;
int n;
int r;
  printf("Enter the value of principal amount:");
  scanf("%d",&p);
   printf("Enter the  number of years:");
  scanf("%d",&n);
   printf("Enter the rate of interest:");
  scanf("%d",&r);
  int SI=p*n*r/100;
  printf("The value simple interest is:%d",SI);
return 0;
}
